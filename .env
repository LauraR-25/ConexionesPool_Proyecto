DB_HOST=localhost
DB_PORT=5433
DB_NAME=javaprueba
DB_USER=postgres
DB_PASSWORD=postgres
POOL_SIZE=15
POOL_TIMEOUT=10000
REINTENTOS_MAXIMOS=1
PETICIONES_POR_DEFECTO=50
QUERIES=SELECT * FROM usuario LIMIT 1; SELECT id, nombre FROM usuario LIMIT 1; SELECT COUNT(*) FROM usuario