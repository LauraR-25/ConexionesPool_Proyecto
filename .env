# Database configuration
DB_HOST=localhost
DB_PORT=5433
DB_NAME=javaprueba
DB_USER=postgres
DB_PASSWORD=jf121206

# Pool configuration
POOL_SIZE=50
POOL_TIMEOUT=3000

# Simulation configuration
REINTENTOS_MAXIMOS=3
PETICIONES_POR_DEFECTO=15000

# Queries de muestra (separadas por punto y coma)
QUERIES=SELECT * FROM usuario LIMIT 1; SELECT id, nombre FROM usuario LIMIT 1; SELECT COUNT(*) FROM usuario